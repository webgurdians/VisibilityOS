# Visibility OS

A public, evidence-backed knowledge system for how AI systems discover, retrieve, understand, cite, recommend, rank, and act on information.

Working domain: `visibilityos.in`

The application is built with Next.js and is designed to read from Neon PostgreSQL through `DATABASE_URL`, with a versioned local seed corpus as a build-safe fallback.

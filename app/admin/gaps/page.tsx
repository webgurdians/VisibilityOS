import { neon } from '@neondatabase/serverless';import AdminNav from '../_components/AdminNav';export const dynamic='force-dynamic';export const metadata={title:'Research Gaps | Research Console',robots:{index:false,follow:false}};export default async function Gaps(){const sql=process.env.DATABASE_URL?neon(process.env.DATABASE_URL):null;const rows=sql?await sql`select id,title,description,priority,status,updated_at from public.research_gaps order by case priority when 'high' then 1 when 'medium' then 2 else 3 end, updated_at desc`:[];return <main className="shell"><section className="hero"><span className="eyebrow">Open questions</span><h1>Research Gaps</h1><p className="lead">Unresolved questions that should drive future sourcing and experiments.</p></section><AdminNav/><div style={{display:'grid',gap:'1rem'}}>{rows.map((r:any)=><article className="card" key={r.id}><div style={{display:'flex',gap:'.5rem',flexWrap:'wrap'}}><span className="pill">{r.priority}</span><span className="pill">{r.status}</span></div><h3>{r.title}</h3><p>{r.description}</p></article>)}</div></main>}
